# Fastq_to_VCF_pipeline
This bioinformatics pipeline is designed to trimm and map reads, and variant call the output. The code is designed to be ran on a High-Performance Cluster running on a debian system to which one can interact using SLURM. The script makes use of parallelization and multi-threading.
